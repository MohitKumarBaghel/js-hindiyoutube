  // maths
   const number1 = 100;
   const number2 = new Number(400);
   //  console.log(number1)
   // console.log(number2.toString().length);

   // console.log(number2.toFixed(2))
  
    
      // console.log(Math.random()) // value return between 0 and 1
       console.log((Math.random()*10) +1) // between 1 and 9
       console.log(Math.floor(Math.random()*10)+1) // between 1 to 9 in single digit
       console.log(Math.round(Math.random()*10)+1)  // between 1 to 9 in single didgit
